// Generated by IcedCoffeeScript 108.0.11
(function() {
  angular.module('app.controllers').controller('AssessmentController', function($scope, $http) {
    var formatDate, publishingStatus, refreshAssessmentButton, useTwoDigits;
    formatDate = function(date) {
      var day, month, year;
      year = date.getFullYear();
      month = useTwoDigits(date.getMonth() + 1);
      day = useTwoDigits(date.getDate());
      return "" + day + "/" + month + "/" + year;
    };
    useTwoDigits = function(val) {
      if (val < 10) {
        return "0" + val;
      }
      return val;
    };
    $scope.assessmentRequest = {};
    $scope.assessmentRequest.id = $scope.form.id;
    publishingStatus = {
      riskAssessment: {
        currentClass: {},
        completed: {}
      },
      signOff: {
        currentClass: {},
        completed: {}
      },
      upload: {
        currentClass: {},
        completed: {}
      },
      currentActiveView: {},
      signOffButtonDisabled: !$scope.user.isIaoUser || ($scope.form.publication !== null && $scope.form.publication.openData.signOff !== null)
    };
    $scope.publishingStatus = publishingStatus;
    $scope.refreshPublishingStatus = function(activeView) {
      publishingStatus.currentActiveView = activeView;
      publishingStatus.riskAssessment.completed = $scope.form.publication !== null && $scope.form.publication.openData.assessment.completed;
      publishingStatus.signOff.completed = $scope.form.publication.openData.signOff !== null;
      publishingStatus.upload.completed = $scope.form.publication.openData.lastSuccess !== null;
      if (activeView === "risk assessment") {
        publishingStatus.riskAssessment.currentClass = publishingStatus.signOff.completed ? "visited" : "current";
        publishingStatus.signOff.currentClass = publishingStatus.signOff.completed ? "current" : "";
        publishingStatus.upload.currentClass = "";
        return console.log("refreshing publishing status " + activeView + " current class " + publishingStatus.riskAssessment.currentClass);
      } else if (activeView === "sign off") {
        console.log("refreshing publishing status " + activeView);
        publishingStatus.riskAssessment.currentClass = "visited";
        publishingStatus.signOff.currentClass = "current";
        return publishingStatus.upload.currentClass = "";
      } else {
        publishingStatus.riskAssessment.currentClass = "visited";
        publishingStatus.signOff.currentClass = "visited";
        return publishingStatus.upload.currentClass = "current";
      }
    };
    $scope.refreshPublishingStatus("risk assessment");
    refreshAssessmentButton = function() {
      if ($scope.form.publication !== null && $scope.form.publication.openData.assessment.completed) {
        if ($scope.form.publication.openData.assessment.initialAssessmentWasDoneOnSpreadsheet) {
          return $scope.assessmentButtonText = "Initial assessment completed on spreadsheet";
        } else {
          return $scope.assessmentButtonText = "Completed by " + $scope.form.publication.openData.assessment.completedByUser.displayName + " on " + formatDate(new Date($scope.form.publication.openData.assessment.completedOnUtc));
        }
      } else {
        return $scope.assessmentButtonText = "I AGREE";
      }
    };
    refreshAssessmentButton();
    if ($scope.form.publication !== null && $scope.form.publication.openData.signOff !== null) {
      $scope.signOffButtonText = "Signed off by " + $scope.form.publication.openData.signOff.user.displayName + " on " + formatDate(new Date($scope.form.publication.openData.signOff.dateUtc));
    } else if ($scope.user.isIaoUser) {
      $scope.signOffButtonText = "SIGN OFF";
    } else {
      $scope.signOffButtonText = "Pending sign off";
    }
    $scope.assessClick = function() {
      return $http.put('../api/publishing/opendata/assess', $scope.assessmentRequest).success(function(result) {
        $scope.status.refresh();
        $scope.expandSignOff();
        $scope.form = result.record;
        return refreshAssessmentButton();
      })["catch"](function(error) {
        $scope.notifications.add(error.data.exceptionMessage);
        return $scope.$dismiss();
      });
    };
    $scope.nextStep = function() {
      if (publishingStatus.riskAssessment.currentClass === "current") {
        $scope.refreshPublishingStatus("sign off");
      } else if (publishingStatus.signOff.currentClass === "current") {
        scope.refreshPublishingStatus("upload");
      }
      return console.log("Show assessment: " + $scope.showAssessmentView + ", show sign off: " + $scope.showSignOffView);
    };
    return $scope.close = function() {
      return $scope.$close($scope.form);
    };
  });

}).call(this);

//# sourceMappingURL=AssessmentController.js.map
